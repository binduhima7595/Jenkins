pipeline {
    agent any

    stages {
        stage('Debug Environment') {
            steps {
                script {
                    def isWindows = !isUnix()
                    if (isWindows) {
                        bat 'echo Hello from Windows!'
                    } else {
                        sh 'echo Hello from Unix!'
                    }
                }
            }
        }
    }
}